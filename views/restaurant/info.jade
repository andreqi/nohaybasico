extends ./restaurant-template.jade

block content
  .container
    .row
        .6u
          .box
            h3.info
              i(class='icon fa-clock-o')
              span Horario de atención
            p #{restaurant.contact.workTime}
            h3.info
              i(class='icon fa-map-marker')
              span Dirección
            p #{restaurant.location.address}
            a(class='view-google-maps', href='#{restaurant.mapsURL}', target='_blank') Ver en google maps  
            h3.info
              i(class='icon fa-phone')
              span Contacto
            p #{restaurant.contact.msg}
            footer
              - var socialLinks = [];
              - var sm = restaurant.contact.social;
              - sm && sm.facebook ? socialLinks.push({url: sm.facebook, icon: 'fa-facebook'} ): null;
              - sm && sm.twitter ? socialLinks.push({url: sm.twitter, icon: 'fa-twitter'} ): null;
              - sm && sm.instagram ? socialLinks.push({url: sm.instagram, icon: 'fa-instagram'} ): null;
              - sm && sm.googlePlus ? socialLinks.push({url: sm.googlePlus, icon: 'fa-google-plus'} ): null;
              - sm && sm.pinterest ? socialLinks.push({url: sm.pinterest, icon: 'fa-pinterest'} ): null;
              ul.social-info
                each link in socialLinks
                  li
                    a(target='_blank', class="fa #{link.icon}",
                      href='#{link.url}')
                      label gg

        .6u
          .box
            - var lat = restaurant.location.coordinates.lat;
            - var lng = restaurant.location.coordinates.lng;
            img(class='img-responsive', src='/restaurant/map/#{lat}/#{lng}')
