div(class='4u')  
  section.box
    a(class='image featured')
      .box-separator
    header
      div.same-line
        h3.same-line #{rest.name}
      div.vertical-middle 
        span S/. #{rest.price_range}
        a.pull-right(class='button small', href='/#{rest.homeID}/info') 
          img(src='/images/walking2.png')
          span #{rest.walking_time}
    .row
      ul(class='row dishes')
        if rest.dish_preview.length
          each dish in rest.dish_preview
            li.12u #{dish.name}
        else
            li.12u .
            li.12u .
            li.12u .
            li.12u .
            li.12u .
    footer
      - var redirect = rest.dishes ? 'menu' : 'carta';
      a(class='button alt', href='#{rest.homeID}/#{redirect}') Ver más
