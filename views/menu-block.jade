li(class='box dishes-block')
  header 
    h3 #{dishBlock.name}
  ul(class='row dishes')
    each dish in dishBlock.dishes
      li.6u
        span #{dish.name}
        ul.same-line
          each tag in dish.in_menu
            li(class='tag #{map[tag]}') #{tag}

  
       